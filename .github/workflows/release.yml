name: 🚀 Release

on:
  workflow_dispatch:
    inputs:
      version_bump:
        description: "Select the version to increment (major, minor, patch)"
        required: true
        default: "patch"
        type: choice
        options:
          - major
          - minor
          - patch

jobs:
  release:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest]

    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      PROJECT_NAME: dependency-injection
      PROJECT_PATH: src/DependencyInjection/DependencyInjection.csproj
      GODOT_ADDON_PATH: src/DependencyInjection.Godot/addons/DependencyInjection

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🛠️ Setup .NET SDK
        uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json

      - name: 📦 Restore Dependencies
        run: dotnet restore $PROJECT_PATH

      - name: 🏗️ Build Project
        run: dotnet build --no-restore --configuration Release $PROJECT_PATH

      - name: 🔖 Get Latest Tag
        id: get_tag
        run: |
          git fetch --tags
          latest_tag=$(git tag --sort=-v:refname | head -n 1)
          if [ -z "$latest_tag" ]; then
            latest_tag="0.0.0"
            echo "No tags found. Using default version: $latest_tag"
          else
            echo "Latest Tag: $latest_tag"
          fi
          echo "latest_tag=$latest_tag" >> $GITHUB_OUTPUT

      - name: 🧮 Calculate Version
        id: calculate_version
        run: |
          current_version="${{ steps.get_tag.outputs.latest_tag }}"
          IFS='.' read -r major minor patch <<< "$current_version"
          case "${{ github.event.inputs.version_bump }}" in
            major) major=$((major + 1)); minor=0; patch=0 ;;
            minor) minor=$((minor + 1)); patch=0 ;;
            patch) patch=$((patch + 1)) ;;
          esac
          echo "PROJECT_VERSION=$major.$minor.$patch" >> $GITHUB_ENV

      - name: 📦 Pack NuGet Package
        run: dotnet pack --no-build --configuration Release /p:PackageVersion=$PROJECT_VERSION -o $RUNNER_TEMP $PROJECT_PATH

      - name: 🚀 Publish NuGet Package
        run: dotnet nuget push $RUNNER_TEMP/$PROJECT_NAME-$PROJECT_VERSION.nupkg --api-key ${{ secrets.NUGET_API_KEY }} --source https://api.nuget.org/v3/index.json

      - name: 📦 Pack Godot Addon
        run: |
          mkdir -p $RUNNER_TEMP/$PROJECT_NAME-$PROJECT_VERSION-godot/DependencyInjection
          cp -r $GODOT_ADDON_PATH/* $RUNNER_TEMP/$PROJECT_NAME-godot-$PROJECT_VERSION/DependencyInjection/
          sed -i 's/version="[^"]*"/version="'$PROJECT_VERSION'"/' $RUNNER_TEMP/$PROJECT_NAME-godot-$PROJECT_VERSION/DependencyInjection/plugin.cfg
          cd $RUNNER_TEMP && zip -r $PROJECT_NAME-$PROJECT_VERSION-godot-addon.zip $PROJECT_NAME-godot-$PROJECT_VERSION -x '*.zip'
        shell: bash

      - name: 🔖 Create Tag and Push
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"
          git tag "$PROJECT_VERSION"
          git push origin "$PROJECT_VERSION"

      - name: 📝 Create GitHub Release
        run: |
          gh release create $PROJECT_VERSION $RUNNER_TEMP/dependency-injection-$PROJECT_VERSION.nupkg $RUNNER_TEMP/$env.PROJECT_NAME-$PROJECT_VERSION-godot-addon.zip \
          --title "dependency-injection-$PROJECT_VERSION" \
          --generate-notes
