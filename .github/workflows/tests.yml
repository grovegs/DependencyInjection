name: 🧪 Tests

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

jobs:
  tests:
    uses: grovegs/workflows/.github/workflows/godot-package-tests.yml@feature/new-workflows
    with:
      core-project: src/GroveGames.DependencyInjection
      godot-project: src/GroveGames.DependencyInjection.Godot
      global-json-file: global.json
